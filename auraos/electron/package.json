{
  "name": "auraos",
  "version": "1.0.0",
  "description": "AI-powered browser",
  "main": "src/main.js",
  "scripts": {
    "start": "electron . --no-sandbox --disable-gpu-sandbox",
    "dev": "electron . --no-sandbox --disable-gpu-sandbox --enable-logging"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}